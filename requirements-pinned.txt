# Exact versions the production runs on the KU Leuven wICE cluster used.
# Captured 2026-08-31 under Python 3.12.3 with:
#   module load SciPy-bundle/2024.05-gfbf-2024a
#   module load tqdm/4.66.5-GCCcore-13.3.0
#
# Note that matplotlib was NOT provided by those modules -- it came from a
# `pip install --user` in ~/.local. Anything reproducing these results needs it
# installed explicitly.
#
# Use requirements.txt for a fresh unpinned install; use this file to reproduce
# the exact numerics of the archived runs.

numpy==1.26.4
scipy==1.13.1
mpmath==1.3.0
tqdm==4.66.5
matplotlib==3.10.8
contourpy==1.3.3
cycler==0.12.1
fonttools==4.61.1
kiwisolver==1.4.9
pillow==12.1.1
